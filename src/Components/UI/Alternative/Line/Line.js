/**
 * This is the Line component of the Alternative sign component.
 * This component is imported in the Alternative component.
 **/

import React from 'react';
import classes from './Line.module.css';

const line = (props) => (
    <div className={classes.Line}></div>
);

export default line;